# Tests

The test suite verifies DOI-complete source-manifest reconstruction,
conservative train-source-overlap exclusion, source-group-aware training CV,
the machine-readable lock-before-test boundary, calibration-only policy
selection, final-test score regeneration, three-way active leakage checks,
trace/hash replay, the 100/20/10 robustness design, figure-source coverage and
full-runner recomputation.
